export declare namespace Meta {
    function set({ author, title, description, image, favicon, }: {
        author?: string;
        title?: string;
        description?: string;
        image?: string;
        favicon?: string;
    }): void;
}
export { Meta as meta };
